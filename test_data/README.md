# LoyaltySystemService - Test Data

Тестовые файлы и инструкции для проверки работы LoyaltySystemService API.

## Содержимое

### 🚀 api_requests.sh
Исполняемый bash-скрипт с curl командами для тестирования API.

**Возможности:**
- Интерактивное меню для выбора групп тестов
- Smoke тесты (основные сценарии)
- Позитивные и негативные сценарии
- Цветной вывод результатов
- Проверка доступности сервиса

---

## Быстрый старт

### 1. Подготовка окружения

```bash
# Запустить все сервисы
make docker-up

# Применить миграции
make migrate-up
```

### 2. Проверка доступности сервиса

```bash
# Проверить, что сервис запущен
curl http://localhost:8084/metrics

# Должен вернуть Prometheus метрики
```

### 3. Запуск тестов

#### Вариант A: Интерактивный режим

```bash
chmod +x test_data/api_requests.sh
./test_data/api_requests.sh
```

**В интерактивном меню:**
- `1-3` - выбрать группу тестов
- `s` - запустить smoke tests
- `a` - запустить все тесты
- `q` - выход

#### Вариант B: Отдельные тесты

```bash
# Загрузить функции
source test_data/api_requests.sh

# Запустить конкретный тест
tc_1_1    # Получение карты лояльности
tc_2_1    # Создание карты лояльности
tc_3_1    # Настройка конфигурации

# Запустить группу тестов
run_group_1    # Get Loyalty Card (TC-1.*)
run_group_2    # Create Loyalty Card (TC-2.*)
```

---

## Структура тестов

### Group 1: Get Loyalty Card (TC-1.*)
- TC-1.1: Получение существующей карты
- TC-1.2: Получение карты несуществующей компании (404)
- TC-1.3: Карта не найдена (404)
- TC-1.4: Конфигурация отключена (404)
- TC-1.5: Невалидные параметры (400)

### Group 2: Create Loyalty Card (TC-2.*)
- TC-2.1: Успешное создание карты
- TC-2.2: Попытка создать дубликат (409)
- TC-2.3: Конфигурация отключена (404)
- TC-2.4: Компания не найдена (404)
- TC-2.5: Невалидные данные (400)

### Group 3: Configure Loyalty (TC-3.*)
- TC-3.1: Создание конфигурации менеджером
- TC-3.2: Обновление конфигурации
- TC-3.3: Включение/отключение программы
- TC-3.4: Попытка настройки не-менеджером (403)
- TC-3.5: Компания не найдена (404)
- TC-3.6: Невалидные данные (400)

---

## Примеры использования

### Пример 1: Получение карты лояльности

```bash
curl -s -X GET "http://localhost:8084/api/v1/loyalty-cards?userId=123456789&companyId=1" \
  -H "Content-Type: application/json" | jq .
```

### Пример 2: Создание карты лояльности

```bash
curl -s -X POST "http://localhost:8084/api/v1/loyalty-cards" \
  -H "Content-Type: application/json" \
  -d '{
    "userId": 123456789,
    "companyId": 1
  }' | jq .
```

### Пример 3: Настройка программы лояльности

```bash
curl -s -X POST "http://localhost:8084/api/v1/companies/1/loyalty-config" \
  -H "Content-Type: application/json" \
  -H "X-User-ID: 777777777" \
  -d '{
    "discountPercentage": 10.5,
    "isEnabled": true
  }' | jq .
```

---

## Зависимости для тестирования

### Обязательные

- **curl** - для HTTP запросов
- **jq** - для форматирования JSON ответов

```bash
# macOS
brew install jq

# Ubuntu/Debian
sudo apt-get install jq

# Проверка
jq --version
```

---

## Интеграция с другими сервисами

### Требуются тестовые данные в SellerService

LoyaltySystemService ожидает, что компании существуют в SellerService.

**Минимальный набор:**

```bash
# Компания 1
# Менеджер: 777777777
```

---

## Troubleshooting

### Сервис недоступен

```
✗ LoyaltySystemService недоступен на http://localhost:8084
```

**Решение:**
```bash
# Проверить статус
docker-compose ps

# Перезапустить
make docker-restart

# Посмотреть логи
make docker-logs-app
```

### jq не установлен

```
✗ jq не установлен. Установите: brew install jq
```

**Решение:**
```bash
brew install jq
```

### Ошибки 404 (company not found)

```
{"message": "компания не найдена"}
```

**Причина:** Нет данных в SellerService.

**Решение:**
- Запустить SellerService
- Создать тестовые компании
- Проверить доступность SellerService из LoyaltySystemService

---

## Ожидаемые результаты

### Smoke Tests (4 теста)

- ✅ TC-1.1: Получение карты → 200 OK, данные карты
- ✅ TC-2.1: Создание карты → 201 Created, ID карты
- ✅ TC-3.1: Настройка конфигурации → 200 OK, конфигурация
- ✅ Метрики → Prometheus метрики доступны

### Критерии успеха

- **Все smoke тесты проходят**
- **Позитивные сценарии возвращают корректные данные**
- **Негативные сценарии возвращают правильные HTTP коды**
- **Время ответа < 100ms для GET, < 200ms для POST**

---

## Следующие шаги

После успешного прохождения тестов:

1. **Проверить метрики**
   ```bash
   curl http://localhost:8084/metrics | grep http_request_duration
   ```

2. **Задокументировать найденные баги**

3. **Подготовить к деплою**

---

## Поддержка

Если возникли проблемы:
1. Проверьте логи: `make docker-logs-app`
2. Проверьте БД: `docker exec -it loyaltysystemservice-db psql -U postgres -d smk_loyaltysystemservice`
3. Пересоздайте окружение: `make db-reset`

**Happy Testing! 🚀**
